"""Few-shot prompt demonstrations for paper and review evaluation.

The JSON files in this package are of type `gpt.evaluate_paper.Demonstration`. In the
`abstract` subdirectory, the files are of type `gpt.demonstrations.CSAbstruct`.

Find the `Demonstration` objects in in `gpt.evaluate_paper.EVALUATE_DEMONSTRATIONS`.
"""
