# API configuration for the Paper Network visualization
VITE_API_URL=http://localhost:8000

# JSON file path for the Paper Explorer test page
# Path is relative to the public/ directory, starting with /
# Example: /papers-large.json (file should be in public/papers-large.json)
# Example: /data/papers.json (file should be in public/data/papers.json)
VITE_XP_DATA_PATH=/data/test-data.json
