# API configuration for the Paper Network visualization
VITE_API_URL=http://localhost:8000

# JSON file path for the Paper Explorer test page
# Path is relative to the deployed base URL (configured in vite.config.ts)
# Will be combined with import.meta.env.BASE_URL at runtime
# Example: data/library.json.gz (file should be in public/data/library.json.gz)
VITE_XP_DATA_PATH=data/library.json.gz
